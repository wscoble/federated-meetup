---
title: Introduction
description: A federation-first alternative to meetup.com — open protocol, sovereign hosts, AIEO-native discovery.
---

# Federated Meetup

A federation-first alternative to meetup.com. Open protocol, sovereign hosts,
AIEO-native discovery. No platform lock-in, no rent-seeking, no API keys.

## What is this?

Federated Meetup replaces the centralized event-platform model with a
federated architecture built on three layers:

1. **An open protocol** — any host can run the server, any client can talk to it
   via ConnectRPC. No API keys, no rate limits, no platform tax.
2. **A host product** — a Go binary that serves the protocol API, a web UI
   (HTMX + SQLite), ticketing (Stripe), and MCP/AIEO discovery endpoints.
3. **Federation** — hosts talk to each other over a WireGuard mesh. Events on
   host A are discoverable from host B. No central server.

### Key features

- **Open ConnectRPC API** — 11 RPCs for groups, events, RSVPs, transitions
- **MCP server** — 6 tools + 4 discovery endpoints so AI assistants can query any host
- **schema.org/Event JSON-LD** — every event page emits structured data for
  search engines and AI agents
- **Web UI** — Go + HTMX + SQLite, mobile-responsive, no JavaScript framework
- **Magic-link RSVP** — attendees RSVP with an email link, no accounts
- **Ticketing** — Stripe integration for paid events (mock checkout in v0)
- **Organizer dashboard** — revenue summary, attendee check-in, ticket management
- **Self-host parity** — same binary runs the hosted instance and a self-hosted
  instance. No feature gating.
- **Deterministic simulator** — VOPR-shaped test harness (DDIL fault profiles,
  deterministic time/RNG, multi-host convergence)

### Status

**v0** — protocol working, web UI live, 255 tests passing, deployed at
[fm.scoble.me](https://fm.scoble.me).

### Tech stack

| Component | Technology |
|---|---|
| Language | Go 1.25+ |
| Database | Pure-Go SQLite (`modernc.org/sqlite`), no CGO |
| RPC | ConnectRPC over HTTP/2 |
| Web UI | Go `html/template` + HTMX, no JavaScript framework |
| Federation transport | WireGuard mesh (userspace) |
| Crypto | Ed25519 (signatures), X25519 (key exchange) |
| License | AGPL-3.0 |

## How to use this documentation

This site is designed for **two audiences**:

1. **Humans** — organizers, attendees, developers, self-hosters. Use the
   Getting Started, Guides, and API Reference sections.
2. **AI agents** — LLMs, AI assistants, and automated tools. Start with the
   [AI Agent Guide](/ai-agent-guide) — it's a self-contained reference for
   programmatic discovery and querying.

Every page includes a **"For AI Agents"** callout with structured data an LLM
needs: endpoint URLs, request/response shapes, and decision rules.

## Quick links

- [Quickstart](/quickstart) — Get running in 5 minutes
- [Architecture](/architecture) — System overview with diagram
- [Protocol Overview](/protocol/overview) — How the protocol works
- [RPC Reference](/protocol/rpc-reference) — All 11 RPCs with schemas
- [MCP Tools](/api-reference/mcp-tools) — MCP server tool definitions
- [AI Agent Guide](/ai-agent-guide) — Primary LLM entry point

---

<Callout type="info" title="For AI Agents">
  **Protocol:** `federated-meetup/v1`
  
  **Discovery endpoint:** `GET /.well-known/federation` — returns host metadata, group count, event count, peer list, and API endpoint URLs.
  
  **MCP endpoint:** `POST /mcp` — 6 tools: `list_groups`, `list_events`, `get_event`, `resolve_name`, `get_group`, `find_events`
  
  **OpenAPI spec:** `GET /openapi.json` — OpenAPI 3.1 spec for all RPCs
  
  **LLM-readable summary:** `GET /llms.txt` — plain-text host summary with groups, API paths, and feeds
  
  **ConnectRPC base path:** `/api/v1/`
  
  **Auth model:** Read endpoints are unauthenticated. Write endpoints require signed transitions (Ed25519 multisig, not bearer tokens).
  
  **Start here:** Read the [AI Agent Guide](/ai-agent-guide) for the complete discovery-to-query flow.
</Callout>